# Copy to `.env.test` (gitignored). Start Postgres: docker compose up -d
DATABASE_URL=postgresql://chatvault:chatvault@127.0.0.1:5433/chatvault_test

# Optional — enables saveChat / searchMyChats e2e (otherwise those tests are skipped).
# OPENAI_API_KEY=
